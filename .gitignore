# Personal, machine-local instructions — never shared
CLAUDE.local.md
.claude/settings.local.json

# Installed copy of skills/commit/hooks/commit-msg. Not committed: a second
# copy of the canonical hook in this repo would be free to drift from it.
# The commit skill reinstalls it per clone.
.githooks/

# Python bytecode from skills/commit/scripts/
__pycache__/
*.py[cod]

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
